% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_outcome_mmlca.R
\name{fit_outcome_mmlca}
\alias{fit_outcome_mmlca}
\title{Regression model with multimorbidity pattern as covariate taking into account of class-uncertainty}
\usage{
fit_outcome_mmlca(
  object,
  formula,
  data,
  model = c("glm", "coxph"),
  family = gaussian(),
  method = c("pmi", "weighted"),
  M = 50,
  class_var = "mm_pattern",
  ref_class = "1",
  conf.level = 0.95,
  exponentiate = F,
  seed = NULL,
  boot = F,
  nboot = NULL,
  nrep = NULL
)
}
\arguments{
\item{object}{poLCA object}

\item{formula}{Two-sided model formula specifying the outcome and covariates.}

\item{data}{Data frame containing the variables used in the model.}

\item{model}{Outcome model. Either `"glm"` for generalized linear models  `"coxph"` for Cox proportional hazards models.}

\item{family}{Family object passed to `glm()` when `model = "glm"`. Defaults to `gaussian()`.}

\item{method}{Method used to account for latent class uncertainty. Either `"pmi"` (posterior multiple imputation) or `"weighted"`}

\item{M}{Number of imputations when `method = "pmi"`.}

\item{class_var}{String containing name of the variable of multimorbidity pattern assignments. Defaults to `"mm_pattern"`.}

\item{ref_class}{Reference pattern used in regression models. Defaults to `"1"`.}

\item{conf.level}{Confidence level for confidence intervals. Defaults to `0.95`.}

\item{exponentiate}{Logical. If `TRUE`, exponentiates model coefficients and confidence intervals (e.g. odds ratios or hazard ratios).}

\item{seed}{Optional random seed for reproducibility.}

\item{boot}{Logical. If `TRUE`, confidence intervals are obtained using bootstrap resampling to take into account of uncertainty from LCA model.}

\item{nboot}{Number of bootstrap samples.}

\item{nrep}{Number of random starting values for bootstrap LCA runs, if applicable.}
}
\description{
Regression model with multimorbidity pattern as covariate taking into account of class-uncertainty
}
